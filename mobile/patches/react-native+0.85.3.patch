diff --git a/node_modules/react-native/ReactCommon/react/renderer/core/graphicsConversions.h b/node_modules/react-native/ReactCommon/react/renderer/core/graphicsConversions.h
index ccc6510..9dfd698 100644
--- a/node_modules/react-native/ReactCommon/react/renderer/core/graphicsConversions.h
+++ b/node_modules/react-native/ReactCommon/react/renderer/core/graphicsConversions.h
@@ -8,6 +8,7 @@
 #pragma once
 
 #include <array>
+#include <string>
 #include <unordered_map>
 
 #include <glog/logging.h>
@@ -68,7 +69,21 @@ inline folly::dynamic toDynamic(const YGValue &dimension)
     case YGUnitPoint:
       return dimension.value;
     case YGUnitPercent:
+#if defined(__cpp_lib_format)
       return std::format("{}%", dimension.value);
+#else
+      {
+        std::string str = std::to_string(dimension.value);
+        size_t last_non_zero = str.find_last_not_of('0');
+        if (last_non_zero != std::string::npos) {
+          str.erase(last_non_zero + 1);
+          if (str.back() == '.') {
+            str.pop_back();
+          }
+        }
+        return str + "%";
+      }
+#endif
   }
 
   return nullptr;
